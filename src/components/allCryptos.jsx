/* {this.state.cryptosID.map((infos) => {
              return (
                <div key={infos.id}>
                  <p>{infos.name}</p>
                  <p>{infos.name}</p>
                </div> */
